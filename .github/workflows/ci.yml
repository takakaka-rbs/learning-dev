name: CI

# feat など作業ブランチへの push・PR のみ実行
# main・dev へのマージは release.yml / release-dev.yml が担う
on:
  push:
    branches-ignore:
      - main
      - dev
  pull_request:
    branches-ignore:
      - main
      - dev

jobs:
  checks:
    name: Checks
    uses: ./.github/workflows/_reusable-checks.yml
